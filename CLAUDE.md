# Claude 작업 지침

## 가이드 문서 참조 원칙 (Single Source of Truth)

이 저장소에서 **Schedule DB 작성 워크플로우**와 관련된 모든 작업은 반드시 아래 로컬 문서를 단일 진실 출처(SoT)로 참조한다.

- **로컬 가이드 (SoT)**: [`docs/schedule-db-workflow-guide.md`](docs/schedule-db-workflow-guide.md)

### 규칙

1. Notion 페이지(`https://www.notion.so/364d8a0a7b5a81eeb4fbf6d96dfc1a6b`)는 **참조 금지**. 워크플로우 절차·속성 정의·예외 처리 등은 모두 로컬 가이드에서만 가져온다.
2. 가이드 변경이 필요하면 **로컬 파일을 먼저 수정**하고 커밋한다. 필요 시 사용자가 Notion 쪽을 직접 따라 갱신한다 (역방향 동기화는 사용자 결정).
3. Schedule DB 항목을 새로 만들거나 갱신할 때 §2 속성 정의, §3 본문 템플릿, §5 예외 케이스, §9 학습 노트(§9-9 Sub Project 미연결 정책 포함)를 그대로 따른다.
4. Notion API 호출(`notion-create-pages`, `notion-update-page`, `notion-fetch` 등)은 여전히 사용 가능하다. "참조 금지"는 가이드 문서 본문에 한정한다 — Notion DB의 실제 데이터(Sub Project, Task DB, Monthly DB, Daily DB row)는 정상적으로 fetch·search·update 한다.

## Notion 데이터 변경 시 행동 원칙 (강력 적용 — 위반 금지)

Notion 워크스페이스의 **모든 쓰기 작업**(페이지 생성, 속성 변경, 본문 수정, 페이지 이동/삭제, relation 연결/해제, 코멘트 추가 등)은 다음 원칙을 따른다. 이 원칙은 어떠한 경우에도 우회·생략되어서는 안 된다.

### 1. 생성·추가 작업은 사전 확인 필수

- 새 페이지를 만들거나 새 블록을 추가하기 전에 **무엇을 어디에 어떤 속성으로 만들 것인지** 사용자에게 명확히 보고하고 승인을 받는다.
- 배치 생성도 동일하다. "N개를 한 번에" 만들 경우, 각 항목의 핵심 속성(Name / Status / Type / Period / Sub Project / 본문 개요)을 사용자가 확인 가능한 형태로 제시한 뒤에만 호출한다.
- 가이드(§2~§9)에 명시된 속성·본문 구조는 그대로 적용하되, **클러스터링·제목·라벨 결정은 임의로 하지 않는다** (가이드 §9-5 "묶기 결정은 사용자 확인 필수" 원칙).

### 2. 삭제·수정 작업은 절대 추정 금지

사용자가 "지워라", "수정해라", "바꿔라", "고쳐라", "정리해라" 같은 지시를 내릴 때 다음을 **반드시** 지킨다.

- **추정 금지**: "사용자가 말한 것은 아마 이것일 것이다", "이런 의미로 해석되니 이렇게 처리하자", "비슷한 항목도 함께 정리하자" 같은 inference로 작업 범위를 넓히거나 좁히지 않는다.
- **동기화 절차**:
  1. 사용자가 말한 대상을 식별한다. 모호하면 그 시점에 멈추고 사용자에게 묻는다 (`AskUserQuestion`).
  2. 변경 전후를 사용자에게 명시한다. 형식 예시:
     - **대상 페이지**: 페이지 URL/ID + 현재 제목
     - **변경 속성/블록**: 어느 속성·어느 블록인지
     - **현재 값** → **목표 값** (값 자체를 그대로 표시)
  3. 사용자가 "그대로 진행해도 좋다"고 명확히 승인하기 전에는 `notion-update-page` / `notion-move-pages` / 본문 삭제 호출을 하지 않는다.
- **다건 작업**: 여러 페이지·여러 속성을 한꺼번에 바꾸라는 지시도, 위 절차에 따라 전체 목록을 보여주고 "이 N건 전부를 이렇게 바꿀까요?"를 명시 확인한 뒤에만 일괄 호출한다. 한 건이라도 사용자 표현과 어긋날 가능성이 있으면 분할 확인한다.
- **되돌릴 수 없는 작업** (페이지 archive, 본문 블록 삭제, relation 끊기 등): 위 절차에 더해 "되돌릴 수 없다"는 점을 사용자에게 명시한다.

### 3. "동기화"의 정의

> **사용자가 원하는 변경 내역**과 **에이전트가 실제로 수행하려는 변경 내역**이 1:1로 일치한다는 사실을, 작업 호출 직전에 사용자와 명시적으로 합의한 상태.

암묵적 합의, 과거 합의의 자동 확장, 유사 상황으로의 유추 적용은 모두 동기화로 간주하지 않는다.

### 4. 위반 시 대응

만약 위 원칙을 위반한 호출이 이미 들어갔다면, **즉시 멈추고** 사용자에게 보고한다. 사후 정당화나 추가 호출로 덮으려 시도하지 않는다.

---

## 변경 이력

- 2026-05-18 — 가이드 SoT를 로컬로 전환. 이전까지 Notion 페이지를 참조했으나 로컬 사본 동기화 이후 로컬을 단일 출처로 정함.
- 2026-05-18 — "Notion 데이터 변경 시 행동 원칙" 섹션 신설. 사용자 명시 동기화 없는 추정 기반 삭제·수정 금지.
